export default async function handler(req){const headers={'Access-Control-Allow-Origin':'*','Access-Control-Allow-Methods':'POST,OPTIONS','Access-Control-Allow-Headers':'Content-Type'};if(req.method==='OPTIONS')return new Response(null,{headers});try{const{prompt,count=1}=await req.json();const url=`https://generativelanguage.googleapis.com/v1beta/models/imagen-3.0-generate-001:predict?key=${process.env.GEMINI_KEY}`;const res=await fetch(url,{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({instances:[{prompt}],parameters:{sampleCount:Math.min(count,4),aspectRatio:"1:1",safetyFilterLevel:"block_few",personGeneration:"allow_adult"}})});if(!res.ok){let msg=`خطأ ${res.status}`;try{const e=await res.json();if(e.error?.message)msg=e.error.message;if(res.status===400)msg='البرومت غير مقبول';if(res.status===403)msg='Imagen 3 غير متاح أو API Key خاطئ';if(res.status===429)msg='تجاوزت الحد اليومي';}catch{}return new Response(JSON.stringify({error:msg}),{status:res.status,headers:{...headers,'Content-Type':'application/json'}});}const data=await res.json();const images=data.predictions?.map(p=>p.bytesBase64Encoded)||[];return new Response(JSON.stringify({images}),{status:200,headers:{...headers,'Content-Type':'application/json'}});}catch(err){return new Response(JSON.stringify({error:err.message}),{status:500,headers:{...headers,'Content-Type':'application/json'}});}}export const config={runtime:'edge'};
